# Klaviyo is parked

FH-369. The shop does not call Klaviyo. Code, scripts, the previous full build, and the replica plan are in `archive/klaviyo/`.

Do not import that folder. Do not recreate `/api/klaviyo`, `/api/identify`, or `/api/track`. Welcome, abandon, replenish, and win-back have no sender.
